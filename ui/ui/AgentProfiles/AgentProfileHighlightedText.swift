import AppKit
import Structure
import SwiftUI

enum AgentProfileTokenColor {
    /// Medium green shared with `$handle` and `[Derrick:$handle]` labels.
    static let darkGreen = Color(red: 0.18, green: 0.62, blue: 0.32)
}
